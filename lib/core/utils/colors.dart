import 'package:flutter/material.dart';

class ColorConstans {
  // Primary green color used throughout the app (hex: #9DC33B)
  static Color primaryGreen = Color(0xff9DC33B);

  // Background color for scaffolds (hex: #F4F4F4)
  static Color scaffoldBackground = Color(0xffF4F4F4);

  // White color (hex: #FFFFFF) - typically used for text or background
  static Color whiteColor = Color(0xffFFFFFF);

  // Light grey color (hex: #E7E7E7) - suitable for subtle background elements
  static Color liteGrey = Color(0xffE7E7E7);

  // Light grey color (hex: #255, 130, 130, 130) - suitable for subtle background elements
  static Color liteGreyshade1 = Color.fromARGB(255, 130, 130, 130);

  // Orange color (hex: #FFBA49) - often used for buttons or highlights
  static Color orangeColor = Color(0xffFFBA49);

  // Red color (hex: #DB3022) - could be used for errors or alerts
  static Color redColor = Color(0xffDB3022);

  // Green color with transparency (ARGB: #FF50AC00) - could be used for success messages
  static Color greenColor = Color.fromARGB(255, 80, 172, 0);
  //main black
  static Color blackMain = Color(0xff222222);
}
